<?xml version="1.0" encoding="utf-8"?>
<layout  xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="presenter"
            type="com.sunmoon.helper.presenter.NotePresenter"/>
        <variable
            name="note"
            type="com.sunmoon.helper.model.NoteInfo"/>

        </data>
<FrameLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:onClick="@{(theView)->presenter.editNote(theView,note)}"
    android:onLongClick="@{(theView)->presenter.removeNote(theView,note)}"
    android:background="@drawable/list_item_back"
    >
    <android.support.v7.widget.CardView
        android:id="@+id/notes_item_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="2dp"
        app:cardElevation="4dp"
        app:cardCornerRadius="2dp"

        >
            <LinearLayout
                android:orientation="vertical"
                android:layout_width="match_parent"
                android:layout_height="wrap_content">
                <!--<TextView-->
                    <!--android:id="@+id/note_label_text"-->
                    <!--android:layout_width="wrap_content"-->
                    <!--android:layout_height="wrap_content"-->
                    <!--android:paddingBottom="4dp"-->
                    <!--android:paddingTop="6dp"-->
                    <!--android:paddingLeft="6dp"-->
                    <!--android:layout_weight="1.0"-->
                    <!--android:text="@{note.label}"-->
                    <!--android:maxLines="14"-->
                    <!--android:ellipsize="end"-->
                    <!--android:lineSpacingExtra="2dp"-->
                    <!--android:textColor="@android:color/black"-->
                    <!--android:textSize="18sp"/>-->
                <TextView
                    android:id="@+id/note_content_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:padding="6dp"
                    android:maxLines="14"
                    android:lineSpacingExtra="2dp"
                    android:text="@{note.content}"
                    android:ellipsize="end"
                    android:textColor="#413f3f"
                    android:textSize="16sp"/>
                <TextView
                    android:id="@+id/note_last_edit_text"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:padding="6dp"
                    android:text="@{String.valueOf(note.createTime)}"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:textColor="?android:textColorSecondary"
                    android:textSize="12sp"/>
            </LinearLayout>

    </android.support.v7.widget.CardView>
    </FrameLayout>
</layout>
